# ZAP baseline rule overrides for dast-dev.yml. Referenced via -c on the zap-baseline.py
# call inside the zaproxy/action-baseline step (rules_file_name input).
#
# Format: <rule id>	<threshold: WARN|FAIL|IGNORE>	<url regex, optional>
#
# Nothing is overridden by default. This template does not currently machine-link ZAP rule
# overrides to the suppression registry, so do not add an IGNORE or threshold downgrade as
# an application-specific bypass. Use the approved security exception process until a
# validated linkage is implemented.
